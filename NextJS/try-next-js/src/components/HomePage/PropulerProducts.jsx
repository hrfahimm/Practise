/** @format */

import wait from "@/utils/wait";

const PropulerProducts = async () => {
    await wait();
    return (
        <div className='mt-5 text-center p-10 font-bold'>PropulerProducts</div>
    );
};

export default PropulerProducts;
